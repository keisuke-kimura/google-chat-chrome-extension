# Chrome ウェブストア 掲載情報（そのまま貼れる原稿）

デベロッパー ダッシュボードの各欄に対応した原稿。
公開前チェックリストは末尾。

---

## ⚠️ 先に決めること: 拡張機能の名前

現在の manifest の名前は **`Chat Booster for Google Chat`** です。

**ウェブストアの審査では、他社（ここでは Google）の商標を名前に含めると、
提携・公式であるかのような誤認を理由に差し戻されることがあります。**
`〜 for Google Chat` は慣習的に使われている形ですが、リスクはゼロではありません。

安全側に倒すなら、名前から製品名を外し、説明文で用途を伝える形をおすすめします。

| | 名前 | リスク |
| --- | --- | --- |
| 現状 | `Chat Booster for Google Chat` | 商標を名前に含む。差し戻しの可能性あり |
| **推奨** | `Chat Booster` | 低い。説明文で「Google Chat 向け」と明記する |

推奨案を採る場合は `manifest.json` の `name` を `Chat Booster` に変更してください
（ストアの表示名も manifest の値が使われます）。

---

## 基本情報

**名前**（45文字以内）

```
Chat Booster
```

**概要 / 短い説明**（132文字以内）

```
Google Chat のメンションを全スペース横断で検知して即通知。未読の一覧とメッセージのブックマーク保存で、Slack のような使い勝手にします。
```

**カテゴリ**: ワークフローと計画（Workflow & Planning）

**言語**: 日本語

---

## 詳細な説明

```
Google Chat の「通知に気づけない」「あとで読み返せない」を解消する拡張機能です。

■ できること

・メンションを即通知
  参加している全スペースと DM を横断して、自分宛の @メンションを検知し、
  デスクトップ通知と通知音でお知らせします。Google Chat のタブを開いて
  いなくても動作します。

・正確な判定
  Google Chat API のメンション情報を使って判定するため、「本文に自分の
  名前が出ただけ」といった誤検知がありません。

・ハイライトキーワード
  任意の単語を登録すると、それを含むメッセージも通知します（Slack の
  ハイライトワード相当）。

・未読の一覧
  未読があるスペースを件数つきで一覧表示します。Google Chat 本体の既読
  状態をそのまま反映するので、Chat で読めば一覧からも消えます。

・ブックマーク保存
  メッセージにカーソルを合わせて「★ 保存」、または Alt+S。あとで読み返す
  メッセージをためておけます（Slack の「後で」相当）。

・スペースのミュート
  通知が不要なスペースは一覧から除外できます。

■ プライバシー

・あなたのデータを収集しません。開発者へ送信しません。
・サーバーを持たず、すべてブラウザ内で完結します。
・広告・解析・トラッキングは一切含みません。

■ 使いはじめる前に（重要）

この拡張機能は、あなた自身の Google Cloud プロジェクトで発行した OAuth
クライアント ID を使って Google Chat API にアクセスします。開発者の認証情報は
含まれておらず、あなたの通信が開発者側を経由することはありません。

そのため、初回に Google Cloud Console での設定が必要です（15 分ほど）。
拡張機能のオプション画面に手順とリンクを用意しています。

必要なもの:
・Google Workspace アカウント（Google Chat を利用できるもの）
・Google Cloud プロジェクト（無料。Chat API 自体に利用料はかかりません）

設定が難しい場合は、以下の手順書をご覧ください。
https://github.com/keisuke-kimura/google-chat-chrome-extension#セットアップ

■ ソースコード

オープンソースです。何をしているかはコードで確認できます。
https://github.com/keisuke-kimura/google-chat-chrome-extension
```

---

## プライバシー関連の申告

**プライバシーポリシーの URL**

```
https://github.com/keisuke-kimura/google-chat-chrome-extension/blob/main/PRIVACY.md
```

**単一用途の説明（Single purpose）**

```
Google Chat の新着メッセージから自分宛のメンションを検知して通知し、
未読状況の一覧表示とメッセージの保存を行う。
```

**データ使用に関する申告（チェック項目）**

収集するデータの種別として、以下を申告してください。

| 項目 | 申告 | 理由 |
| --- | --- | --- |
| 個人を特定できる情報 | **はい** | 自分宛か判定するため、ユーザー ID・氏名・メールアドレスを取得 |
| 認証情報 | **はい** | OAuth トークンをローカルに保存 |
| 個人的なやり取り | **はい** | Chat のメッセージ本文を取得 |
| 位置情報 / 健康情報 / 財務情報 / ウェブ閲覧履歴 | いいえ | 取得しません |

3つの証明にすべてチェックを入れてください（いずれも事実です）。

- 承認された用途以外にデータを使用または転送していません
- 第三者に販売していません
- 信用調査や融資目的で使用または転送していません

---

## 権限の理由（Permission justification）

各権限の入力欄にそのまま貼れる文面です。

**`identity`**
```
ユーザー自身が用意した OAuth クライアント ID を使い、Google アカウントで
Google Chat API の利用を認可してもらうために使用します。
```

**`storage`**
```
ユーザーの設定、OAuth トークン、取得したメンション・未読・保存済みメッセージを
ローカルに保存するために使用します。外部への送信は行いません。
```

**`alarms`**
```
Google Chat API には拡張機能へ通知を配信する仕組みがないため、一定間隔で
新着メッセージを確認する必要があります。そのスケジュールに使用します。
```

**`notifications`**
```
検知したメンションをデスクトップ通知でユーザーに知らせるために使用します。
```

**`offscreen`**
```
通知音を再生するために使用します。Service Worker では音声を再生できないため、
Web Audio を実行する offscreen document が必要です。
```

**`sidePanel`**
```
メンション・未読スペース・保存済みメッセージの一覧を表示する UI として
使用します。
```

**`tabs`**
```
通知や一覧の項目をクリックした際に、すでに開いている Google Chat のタブを
探して再利用し、該当メッセージへ移動するために使用します。
```

**ホスト権限 `https://chat.googleapis.com/*`, `https://www.googleapis.com/*`, `https://oauth2.googleapis.com/*`**
```
Google Chat API の呼び出しと OAuth 認証のために使用します。
```

**ホスト権限 `https://chat.google.com/*`, `https://mail.google.com/*`**
```
Google Chat の画面上でメッセージに「★ 保存」ボタンを表示し、新着を素早く
検知するためのコンテンツ スクリプトを実行します。
```

**リモートコードの使用**
```
使用していません。すべてのコードは拡張機能のパッケージに含まれています。
```

---

## 用意が必要な画像

| 用途 | サイズ | 必須 |
| --- | --- | --- |
| アイコン | 128×128 PNG | ○（`icons/icon128.png` を使用） |
| スクリーンショット | 1280×800 または 640×400 | ○（最低1枚、最大5枚） |
| 小さいプロモーション タイル | 440×280 | 任意 |

**スクリーンショットの案**（実際に動かして撮影が必要です）

1. サイドパネルの「メンション」タブ（一覧が入った状態）
2. サイドパネルの「未読」タブ（件数バッジが見える状態）
3. デスクトップ通知が出ている様子
4. Chat のメッセージにホバーして「★ 保存」ボタンが出ている様子
5. オプション画面（設定項目が見える状態）

> 実データを写す場合は、社名・個人名・業務内容が読めないよう
> ダミーのスペースを作って撮影するか、マスクしてください。

---

## 公開手順

1. [デベロッパー ダッシュボード](https://chrome.google.com/webstore/devconsole) に登録
   （初回のみ $5 の買い切り）
2. `bash tools/pack.sh` で `dist/chat-booster-<version>.zip` を作成
3. 「新しいアイテム」→ zip をアップロード
4. **この時点で表示される「アイテム ID」を控える**
5. 上記の掲載情報・プライバシー申告・権限の理由を入力
6. スクリーンショットをアップロード
7. 公開範囲を選択
   - **限定公開（Unlisted）**: リンクを知っている人だけ
   - **一般公開（Public）**: 検索に出る
8. 審査に提出（数日〜2週間程度）

---

## ⚠️ 公開後に必ず必要な作業: リダイレクト URI

**ウェブストアで発行される拡張機能 ID は、ローカルで読み込んでいたときの ID と異なります。**
そのため OAuth のリダイレクト URI も変わります。

```
公開前（ローカル）: https://<ローカルのID>.chromiumapp.org/
公開後（ストア）  : https://<アイテムID>.chromiumapp.org/
```

**手順 4 でアイテム ID を控えておけば、審査中に先回りして登録できます。**
Google Cloud Console の OAuth クライアントの「承認済みのリダイレクト URI」に、
ストア版の URI を**追加**してください（ローカル版も残しておけば開発を続けられます）。

利用者側も同様に、それぞれの OAuth クライアントにストア版の URI を登録する必要が
あります。この値は拡張機能のオプション画面にコピーボタン付きで表示されるので、
利用者が迷うことはありません。

---

## 審査で指摘されやすい点

- **プライバシーポリシーの URL が未設定** — 必須です。上記の URL を入れてください
- **権限の理由が空欄** — すべての権限に理由が必要です
- **説明と実装の不一致** — 単一用途の説明が実装と合っているか確認されます
- **名前に他社商標** — 冒頭の名前の項を参照
- **広いホスト権限** — `chat.google.com` / `mail.google.com` に限定済みで、
  理由も明記してあるため通りやすい構成です
